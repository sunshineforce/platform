<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.stat.StaExceptionDayDao">

    <resultMap type="com.platform.entity.stat.StaExceptionDayEntity" id="staExceptionDayMap">
        <result property="id" column="id"/>
        <result property="statDate" column="stat_date"/>
        <result property="pendingNum" column="pending_num"/>
        <result property="reviewNum" column="review_num"/>
        <result property="finishNum" column="finish_num"/>
    </resultMap>

	<select id="queryObject" resultType="com.platform.entity.stat.StaExceptionDayEntity">
		select
			`id`,
			`stat_date`,
			`pending_num`,
			`review_num`,
			`finish_num`
		from sta_exception_day
		where id = #{id}
	</select>

	<select id="queryList" resultType="com.platform.entity.stat.StaExceptionDayEntity">
		select
    		`id`,
    		`stat_date`,
    		`pending_num`,
    		`review_num`,
    		`finish_num`
		from sta_exception_day
		WHERE 1=1
		<if test="startTime != null and endTime != null">
			<![CDATA[	AND #{statDate} <= `stat_date` <= #{statDate} ]]>
		</if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from sta_exception_day
		WHERE 1=1
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
	</select>
	 
	<insert id="save" parameterType="com.platform.entity.stat.StaExceptionDayEntity" useGeneratedKeys="true" keyProperty="id">
		insert into sta_exception_day(
			`stat_date`,
			`pending_num`,
			`review_num`,
			`finish_num`)
		values(
			#{statDate},
			#{pendingNum},
			#{reviewNum},
			#{finishNum})
	</insert>
	 
	<update id="update" parameterType="com.platform.entity.stat.StaExceptionDayEntity">
		update sta_exception_day 
		<set>
			<if test="statDate != null">`stat_date` = #{statDate}, </if>
			<if test="pendingNum != null">`pending_num` = #{pendingNum}, </if>
			<if test="reviewNum != null">`review_num` = #{reviewNum}, </if>
			<if test="finishNum != null">`finish_num` = #{finishNum}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from sta_exception_day where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from sta_exception_day where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>