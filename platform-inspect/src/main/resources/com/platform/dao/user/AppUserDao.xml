<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.user.AppUserDao">

    <resultMap type="com.platform.entity.user.AppUserEntity" id="appUserMap">
        <result property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="mobile" column="mobile"/>
        <result property="identify" column="identify"/>
        <result property="superior" column="superior"/>
        <result property="account" column="account"/>
        <result property="password" column="password"/>
        <result property="certificateUrl" column="certificate_url"/>
        <result property="enabled" column="enabled"/>
        <result property="createTime" column="create_time"/>
        <result property="creator" column="creator"/>
        <result property="updateTime" column="update_time"/>
        <result property="updator" column="updator"/>
    </resultMap>

	<select id="queryObject" resultType="com.platform.entity.user.AppUserEntity">
		select
			`id`,
			`user_name`,
			`mobile`,
			`identify`,
			`superior`,
			`account`,
			`password`,
			`certificate_url`,
			`enabled`,
			`create_time`,
			`creator`,
			`update_time`,
			`updator`
		from app_user
		where id = #{id}
	</select>

	<select id="queryList" resultType="com.platform.entity.user.AppUserEntity">
		select
    		`id`,
    		`user_name`,
    		`mobile`,
    		`identify`,
    		`superior`,
    		`account`,
    		`password`,
    		`certificate_url`,
    		`enabled`,
    		`create_time`,
    		`creator`,
    		`update_time`,
    		`updator`
		from app_user
		WHERE 1=1
		<if test="name != null and name.trim() != ''">
			AND name LIKE concat('%',#{name},'%')
		</if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from app_user
		WHERE 1=1
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
	</select>
	 
	<insert id="save" parameterType="com.platform.entity.user.AppUserEntity" useGeneratedKeys="true" keyProperty="id">
		insert into app_user(
			`user_name`,
			`mobile`,
			`identify`,
			`superior`,
			`account`,
			`password`,
			`certificate_url`,
			`enabled`,
			`create_time`,
			`creator`,
			`update_time`,
			`updator`)
		values(
			#{userName},
			#{mobile},
			#{identify},
			#{superior},
			#{account},
			#{password},
			#{certificateUrl},
			#{enabled},
			#{createTime},
			#{creator},
			#{updateTime},
			#{updator})
	</insert>
	 
	<update id="update" parameterType="com.platform.entity.user.AppUserEntity">
		update app_user 
		<set>
			<if test="userName != null">`user_name` = #{userName}, </if>
			<if test="mobile != null">`mobile` = #{mobile}, </if>
			<if test="identify != null">`identify` = #{identify}, </if>
			<if test="superior != null">`superior` = #{superior}, </if>
			<if test="account != null">`account` = #{account}, </if>
			<if test="password != null">`password` = #{password}, </if>
			<if test="certificateUrl != null">`certificate_url` = #{certificateUrl}, </if>
			<if test="enabled != null">`enabled` = #{enabled}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="creator != null">`creator` = #{creator}, </if>
			<if test="updateTime != null">`update_time` = #{updateTime}, </if>
			<if test="updator != null">`updator` = #{updator}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from app_user where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from app_user where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>